# Use the input() function to read input from the keyboard.

# NOTE: All data is read as a string, if you want an integer, float, or other data type,
#  you will need to use a conversion function to convert the value to the correct data type.


width: float = float(input('Enter the width of your rectangle: '))
height: float = float(input('Enter the height of your rectangle: '))

age: int = int(input('Enter your age: '))

name: str = input('Enter your name: ')